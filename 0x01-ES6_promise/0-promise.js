/* eslint-disable no-unused-vars */
export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // For now, the promise can be resolved immediately
    resolve('API response');
  });
}
